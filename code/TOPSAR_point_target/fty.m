% ========================================================================
% Chirp Scaling Algorithm - Fourier Transform in Range

% ========================================================================

function fs=fty(s);
% fs=fftshift(fft(fftshift(s.'))).';
fs=fftshift(fft(ifftshift(s,2),[],2),2);
% = end ==================================================================
